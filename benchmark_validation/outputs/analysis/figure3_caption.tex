\caption{\textbf{Validation results and RMSD decomposition.} Per-site RMSD distributions for internal-coordinate seed placement (S0), the oracle-best of 12 scanned orientations (S1), the orientation selected by scoring before minimisation (S2), the independently minimised top-ranked pose (S3), and the best of three independently minimised ranked poses for TPO ($n=332$), SEP ($n=241$), and PTR ($n=217$) single-site phosphosites. Median values are annotated above each box. Dashed lines indicate 1.0~\AA\ and 1.5~\AA\ thresholds. Across all residue types, 94.7\% of sites are recovered within 1.5~\AA\ by at least one of the three minimised outputs.}
